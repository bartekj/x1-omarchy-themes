# {{ display_name }} notifications: quiet surface card with a hairline border
# (the old mako identity). Urgency color stays with the shell defaults.
background       = "{{ surface }}"
background-alpha = {{ pill_alpha }}
text             = "{{ foreground }}"
border           = "{{ border_soft }}"
border-alpha     = {{ pill_border_alpha }}
countdown        = "{{ accent }}"
